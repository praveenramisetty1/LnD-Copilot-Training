# .github/skills — Reusable Workflow Skills

Reusable GitHub Actions job snippets for this project.

| Skill | Purpose |
|-------|---------|
| `setup-python.yml` | Install Python 3.11 + pip cache |
| `run-tests.yml` | Run pytest with coverage |
| `docker-build.yml` | Build + smoke-test Docker image |
